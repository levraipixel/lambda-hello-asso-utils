services:
  lambda:
    image: public.ecr.aws/lambda/nodejs:24
    volumes:
      - ./src:/var/task
      - ./layer/nodejs:/opt/nodejs
    env_file:
      - .env
    ports:
      - "9000:8080"
    command: [ "index.handler" ]
